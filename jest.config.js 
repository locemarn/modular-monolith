import { baseConfig } from './jest.config.base.js'

export default {
  ...baseConfig,
  rootDir: '.',
  roots: ['<rootDir>/apps'],
  collectCoverageFrom: [
    'apps/**/*.ts',
    '!apps/**/*.spec.ts',
    '!apps/**/*.e2e-spec.ts',
    '!apps/**/main.ts',
    '!apps/**/*.module.ts',
    '!apps/**/migrations/**',
    '!apps/**/test-utils/**',
  ],
  coverageDirectory: './coverage',
  coverageReporters: ['text', 'lcov', 'html'],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 85,
      lines: 85,
      statements: 85,
    },
  },
  projects: [
    {
      displayName: 'unit',
      rootDir: '.',
      roots: ['<rootDir>/apps'],
      testMatch: ['**/apps/**/__tests__/unit/**/*.spec.ts'],
      testEnvironment: 'node',
      transform: {
        '^.+\\.(t|j)s$': 'ts-jest',
      },
    },
    {
      displayName: 'integration',
      rootDir: '.',
      roots: ['<rootDir>/apps'],
      testMatch: ['**/apps/**/__tests__/integration/**/*.spec.ts'],
      testEnvironment: 'node',
      transform: {
        '^.+\\.(t|j)s$': 'ts-jest',
      },
    },
    {
      displayName: 'e2e',
      rootDir: '.',
      roots: ['<rootDir>/apps'],
      testMatch: ['**/apps/**/__tests__/e2e/**/*.spec.ts'],
      testEnvironment: 'node',
      transform: {
        '^.+\\.(t|j)s$': 'ts-jest',
      },
    },
  ],
}